'use strict';

describe('ari controllers', function() {
  describe('AriCtrl', function() {
    beforeEach(module('ariApp'));

    it('should succeed', inject(function($controller) {
      expect(false).toBe(false);
    }));
  });
});
